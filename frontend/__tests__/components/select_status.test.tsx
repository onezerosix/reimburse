describe('SelectStatus', () => {
  it('should render Status dropdown (with correct values)', () => {
    // TODO
  })

  it('should have onStatusChange prop used with onValueChange of Select', () => {
    // TODO
  })

  it('should convert onValueChange of Select to a number before calling onStatusChange', () => {
    // TODO (3 versions for separate values)
  })

  it('should send "undefined" to onStatusChange if "All" is selected in Status', () => {
    // TODO
  })

  it('should have optional enableAll prop that defaults to false', () => {
    // TODO (i.e. should hide "All" SelectItem by default)
  })

  it('should hide "All" SelectItem when enableAll is false', () => {
    // TODO
  })

  it('should show "All" SelectItem when enableAll is true', () => {
    // TODO
  })

  it('should have no SelectValue placeholder value by default', () => {
    // TODO
  })

  it('should convert and use ReimbursementStatus value when placeholder prop is provided', () => {
    // TODO (3 versions for separate values)
  })
})
